dist/
storybook-static/
coverage/
package-lock.json

# Planning documents authored outside the toolchain. They are already formatted deliberately;
# reformatting them would create churn unrelated to any code change.
#
# Anchored at the repository root: a bare `docs/` also matches `src/docs/`, which holds the
# Storybook documentation pages and is part of the toolchain like anything else under `src`.
CLAUDE.md
/docs/
